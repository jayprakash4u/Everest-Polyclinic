/**
 * Shipped defaults for the one-off homepage settings held in HomePageSetting.
 *
 * These are what renders before anyone edits anything, and what an admin gets
 * back by clearing a field. Keys are shared by the public page, the API and the
 * editor, so they live here rather than being typed out in three places.
 */
export const HOME_SETTING_DEFAULTS = {
  careTeamImage: "/images/sidedoctor.png",
};
